@model PGM.Web.Areas.PGM.Models.TaxAdvancedPaid.AdvancedTaxPaidViewModel
@using PGM.Web.Helpers

@if (Model != null && Model.AdvanceDetailList!= null)
{
    foreach (var x in Model.AdvanceDetailList)
    {
        @Html.Partial("_PartialDetail", x);
    }

}